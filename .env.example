# 访问令牌（REST 与 MCP 共用），公网部署务必改成强随机串
API_TOKEN=dev-token

# SQLite 数据库路径（容器内默认 /data/ledger.db，volume 持久化）
# DB_PATH=/data/ledger.db

# 期初现金余额（元）
INITIAL_BALANCE=0

# 允许跨域来源（仅前后端分离开发时需要，如 http://localhost:5173）
# CORS_ORIGINS=http://localhost:5173
