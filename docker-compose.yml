services:
  ledger:
    build: .
    container_name: ai-ledger
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ledger-data:/data

volumes:
  ledger-data:
